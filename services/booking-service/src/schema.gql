# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Booking {
  id: ID!
  userId: ID!
  rideId: ID!
  status: BookingStatus!
  paymentType: PaymentType!
  pickupLocation: String!
  dropoffLocation: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""Status of a booking"""
enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  REJECTED
}

"""Type of payment for booking"""
enum PaymentType {
  CASH
  CREDIT
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type BookingWithPaymentInfo {
  booking: Booking!
  paymentRequired: Boolean
  paymentUrl: String
}

type Query {
  AllBookings: [Booking!]!
  MyBookings: [Booking!]!
  getBooking(id: ID!): Booking!
}

type Mutation {
  BookRide(data: CreateBookingInput!): BookingWithPaymentInfo!
  cancelBooking(id: ID!): Booking!
  acceptBooking(id: ID!): Booking!
  rejectBooking(id: ID!): Booking!
  updateBookingAfterPayment(bookingId: ID!, status: String!): Booking!
}

input CreateBookingInput {
  userId: ID
  rideId: ID!
  pickupLocation: String!
  dropoffLocation: String!
  paymentType: PaymentType!
  status: BookingStatus
}